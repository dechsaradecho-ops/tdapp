fastapi==0.115.0
uvicorn[standard]==0.30.6
# ยืดหยุ่นเวอร์ชัน: pydantic-core ต้องมี prebuilt wheel ตามเวอร์ชัน Python ที่ Render ใช้
pydantic>=2.9.2,<3
pydantic-settings>=2.5.2,<3
python-dotenv==1.0.1
httpx==0.27.2
redis==5.0.8
# supabase >= 2.15 รองรับคีย์รูปแบบใหม่ sb_secret_ (2.9.1 บังคับ JWT เท่านั้น
# และปฏิเสธ sb_secret_ ทันทีใน create_client ด้วย "Invalid API key" ก่อนยิง network)
supabase>=2.15.0
# postgrest ถูกดึงอัตโนมัติโดย supabase (ชื่อเก่า postgrest-py เลิกใช้แล้ว)
APScheduler==3.10.4
python-json-logger==2.0.7
MetaTrader5==5.0.6147 ; sys_platform == "win32"
